import React from 'react';
import { Link } from 'react-router-dom';

const Home = () => (
  <div className='container grid grid-cols-1 justify-items-center min-h-full'>
    <h1>Accueil</h1>
    <p>veuiller répondre au formulaire</p>
  </div>
);

export default Home;
